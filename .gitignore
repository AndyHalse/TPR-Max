node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Security: Prevent cookie and session files from being committed
*.cookies.txt
*cookies.txt
auth_cookies.txt
session_data.txt
*.session
session_*
*_session*
connect.sid
.connect.sid

# Security: Prevent sensitive auth files
*.auth
.env.local
.env.production

# Security: Prevent user data and credentials from being committed
data/